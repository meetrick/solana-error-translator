# ---------------------------------------------------------------
# Solana Error Translator — Backend Environment Variables
# ---------------------------------------------------------------
# 1. Copy this file and rename it to `.env`  (cp .env.example .env)
# 2. Replace the placeholder below with your own Solana RPC URL.
#    You can use a public endpoint (slower/rate-limited) or a
#    private one from providers like Helius, QuickNode, or Alchemy.
# ---------------------------------------------------------------

SOLANA_RPC_URL=https://api.mainnet-beta.solana.com
